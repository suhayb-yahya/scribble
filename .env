# Admin: password required to access /admin and manage jobs.
# Set this in .env.local (never commit .env.local).
ADMIN_PASSWORD=p@$$w0rd

# Optional: secret used to sign the admin session cookie (defaults to ADMIN_PASSWORD).
# Use a different long random string in production for better security.
# ADMIN_SECRET=your-random-secret
