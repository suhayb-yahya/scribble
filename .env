# Admin: password required to access /admin and manage jobs.
# Set this in .env.local (never commit .env.local).
ADMIN_PASSWORD=p@$$w0rd

# Contact form: email where submissions are sent
# Resend free tier: you can only send TO the same email you signed up with (e.g. suhayb.e.yahya@gmail.com).
# After verifying a domain at https://resend.com/domains you can send to any address.
CONTACT_EMAIL=riyad@scribblestudio.co

# Resend API key (https://resend.com → API Keys)
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional: custom "From" address. Only works after you verify a domain at resend.com/domains.
# Example: CONTACT_FROM_EMAIL=Scribble <noreply@yourdomain.com>


